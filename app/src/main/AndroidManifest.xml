<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.children_health_card">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddOperationsActivity"></activity>
        <activity android:name=".OperationsInfoActivity" />
        <activity android:name=".VisitsInfoActivity" />
        <activity android:name=".AddVisitActivity" />
        <activity android:name=".VaccinationInfoActivity" />
        <activity android:name=".AddVaccinationActivity" />
        <activity android:name=".MedicinesInfoActivity" />
        <activity android:name=".AddMedicineActivity" />
        <activity android:name=".AllergyInfoActivity" />
        <activity android:name=".AddAllergyActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddChildActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ChildListActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>